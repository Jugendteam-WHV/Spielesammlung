{
  "name": "Typst Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  "postCreateCommand": "curl -fsSL https://typst.community/typst-install/install.sh | sh && sudo apt-get update && sudo apt-get install -y fonts-noto",

  "customizations": {
    "vscode": {
      "extensions": [
        "myriad-dreamin.tinymist",
        "tamasfe.even-better-toml"
      ]
    }
  }
}
