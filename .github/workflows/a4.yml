name: Build A4
on: [push]
jobs:
  build_latex:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v4
      - name: Compile A4
        uses: xu-cheng/latex-action@v3
        with:
          root_file: Jugendteam_Spielesammlung_A4.tex
          working_directory: ./A4
          args: -pdf -file-line-error -interaction=nonstopmode
        continue-on-error: true
      - name: Upload A4 PDF file
        uses: actions/upload-artifact@v4
        with:
          name: PDF
          path: A4/Jugendteam_Spielesammlung_A4.pdf
        continue-on-error: true
      - name: Release
        uses: softprops/action-gh-release@v2
        if: github.ref_type == 'tag'
        with:
          repository: Jugendteam-WHV/Spielesammlung
          # note you'll typically need to create a personal access token
          # with permissions to create releases in the other repo
          token: ${{ secrets.Actions }}
          files: A4/Jugendteam_Spielesammlung_A4.pdf

